# fld

Fast Library Design — RNA/DNA sequence library design for MaP-seq.

## Building

```bash
./configure          # build and install to bin/
./configure --clean  # clean rebuild
```

## Testing

```bash
cd build && ctest --output-on-failure
```
